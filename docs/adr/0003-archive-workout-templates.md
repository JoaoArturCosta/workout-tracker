# Archive workout templates instead of deleting them

Removing a workout template will archive it and hide it from new workout choices while preserving active workouts, completed history, and analytics. Archived templates can be restored; any permanent purge must use a separate explicit flow because the prior database cascade deleted all linked workout data.
